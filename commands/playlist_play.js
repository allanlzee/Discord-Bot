const ytdl = require('ytdl-core'); 
const ytSearch = require('yt-search');  
const { execute } = require('./playlist_create');

module.exports = {
    name: 'playlist_play', 
    description: "plays a playlist created in the server", 

    async execute(message, args, servers) {
        
    }
}